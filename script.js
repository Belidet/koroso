// =====================================================
// --- LANGUAGE TRANSLATION SYSTEM ---
// =====================================================

const translations = {
    en: {
        // Navigation
        nav_how: "How it Works",
        nav_services: "Services",
        nav_why: "Why KOROSO",
        nav_download: "Download App",
        
        // Hero
        hero_title: 'Find the Right Skills, <span class="highlight">Right Now.</span>',
        hero_subtitle: "KOROSO connects you with verified local professionals—from accountants to technicians—instantly. Whether you need a service or want to offer one, we make it happen.",
        btn_download: "Download for Android",
        btn_learn: "Learn More",
        
        // How it Works
        how_title: "How It Works",
        how_subtitle: "Simple steps to get your job done or find your next gig.",
        step1_title: "1. Create Profile",
        step1_desc: "Sign up as a client looking for help or a provider showcasing your skills.",
        step2_title: "2. Browse Services",
        step2_desc: "Explore a wide range of categories or let clients find you based on your profession.",
        step3_title: "3. Connect & Hire",
        step3_desc: "Chat securely, agree on terms, and get the job done efficiently.",
        
        // Services
        services_title: "Available Professionals",
        services_subtitle: "Whatever you need, KOROSO has a verified expert for it.",
        serv_accountant: "Accountants",
        serv_accountant_desc: "Tax prep, bookkeeping, and financial advice.",
        serv_cleaner: "House Cleaners",
        serv_cleaner_desc: "Deep cleaning, regular maintenance, and moving help.",
        serv_tech: "Electronics Technicians",
        serv_tech_desc: "Repairs for TVs, phones, computers, and appliances.",
        serv_plumber: "Plumbers",
        serv_plumber_desc: "Leak fixes, pipe installation, and emergency repairs.",
        serv_painter: "Painters",
        serv_painter_desc: "Interior and exterior painting for homes and offices.",
        serv_mover: "Movers",
        serv_mover_desc: "Safe and efficient relocation services.",
        
        // Why KOROSO
        why_title: "Why Choose KOROSO?",
        why1_title: "Verified Profiles",
        why1_desc: "We verify IDs and skills so you can hire with confidence.",
        why2_title: "Secure Messaging",
        why2_desc: "Communicate safely within the app before sharing personal details.",
        why3_title: "Local Professionals",
        why3_desc: "Find talent right in your neighborhood for faster service.",
        
        // Download Section
        dl_title: "Ready to get started?",
        dl_subtitle: "Download the KOROSO app today and join the community.",
        dl_button: "Install KOROSO APK",
        dl_note: "Android 8.0+ required. Safe and secure installation.",
        
        // Footer
        footer_copy: "&copy; 2024 KOROSO. All rights reserved.",
        
        // Modal
        modal_title: "Install KOROSO",
        modal_intro: "To install the app, please follow these steps:",
        modal_step1: 'Tap the <strong>Download APK</strong> button below.',
        modal_step2: 'If prompted, allow installation from <strong>"Unknown Sources"</strong> in your browser settings.',
        modal_step3: "Open the downloaded file to install KOROSO.",
        modal_secure: "Your security is our priority. The file is scanned.",
        modal_btn: "Download APK Now",
        modal_downloading: "Preparing Download...",
        modal_downloaded: "Downloaded! Check Notifications",
        modal_retry: "Try Again",
        modal_android_only: "Android Only",
        modal_toast_title: "📲 Next Steps:",
        modal_toast_steps: "1. Open your <b>Notifications</b> or <b>Downloads</b><br>2. Tap the <b>KOROSO.apk</b> file<br>3. Allow installation if prompted<br>4. Open KOROSO and start hiring!"
    },
    am: {
        // Navigation
        nav_how: "እንዴት እንደሚሰራ",
        nav_services: "አገልግሎቶች",
        nav_why: "ለምን KOROSO",
        nav_download: "አፑን ያውርዱ",
        
        // Hero
        hero_title: 'ትክክለኛውን ክህሎት ያግኙ፣ <span class="highlight">ወዲያውኑ።</span>',
        hero_subtitle: "KOROSO ከተረጋገጡ የአካባቢ ባለሙያዎች ጋር ወዲያውኑ ያገናኝዎታል—ከሂሳብ ባለሙያዎች እስከ ቴክኒሻኖች። አገልግሎት ከፈለጉ ወይም ማቅረብ ከፈለጉ፣ እኛ እናደርገዋለን።",
        btn_download: "ለ Android ያውርዱ",
        btn_learn: "ተጨማሪ ይወቁ",
        
        // How it Works
        how_title: "እንዴት እንደሚሰራ",
        how_subtitle: "ስራዎን ለማከናወን ወይም ቀጣዩን ስራዎን ለማግኘት ቀላል ደረጃዎች።",
        step1_title: "1. መገለጫ ይፍጠሩ",
        step1_desc: "እርዳታ የሚፈልግ ደንበኛ ወይም ክህሎትዎን የሚያሳይ አቅራቢ ሆነው ይመዝገቡ።",
        step2_title: "2. አገልግሎቶችን ያስሱ",
        step2_desc: "ሰፊ ምድቦችን ያስሱ ወይም ደንበኞች በሙያዎ እንዲያገኙዎት ይፍቀዱ።",
        step3_title: "3. ይገናኙ እና ይቅጠሩ",
        step3_desc: "በደህንነት ይወያዩ፣ በውሎች ይስማሙ እና ስራውን በብቃት ያጠናቅቁ።",
        
        // Services
        services_title: "የሚገኙ ባለሙያዎች",
        services_subtitle: "የሚፈልጉት ምንም ይሁን፣ KOROSO የተረጋገጠ ባለሙያ አለው።",
        serv_accountant: "የሂሳብ ባለሙያዎች",
        serv_accountant_desc: "የታክስ ዝግጅት፣ የመዝገብ አያያዝ እና የፋይናንስ ምክር።",
        serv_cleaner: "የቤት ጽዳት ሰራተኞች",
        serv_cleaner_desc: "ጥልቅ ጽዳት፣ መደበኛ ጥገና እና የመጓጓዣ እርዳታ።",
        serv_tech: "የኤሌክትሮኒክስ ቴክኒሻኖች",
        serv_tech_desc: "ለቲቪ፣ ስልኮች፣ ኮምፒውተሮች እና መሳሪያዎች ጥገና።",
        serv_plumber: "የቧንቧ ባለሙያዎች",
        serv_plumber_desc: "የመፍሰሻ ጥገና፣ የቧንቧ ተከላ እና የአደጋ ጊዜ ጥገና።",
        serv_painter: "ቀለም ቀቢዎች",
        serv_painter_desc: "ለቤቶች እና ቢሮዎች የውስጥ እና የውጭ ቀለም።",
        serv_mover: "ጠቃሚ ተሸካሚዎች",
        serv_mover_desc: "ደህንነቱ የተጠበቀ እና ቀልጣፋ የመጓጓዣ አገልግሎቶች።",
        
        // Why KOROSO
        why_title: "ለምን KOROSO ይምረጡ?",
        why1_title: "የተረጋገጡ መገለጫዎች",
        why1_desc: "መታወቂያዎችን እና ክህሎቶችን እናረጋግጣለን ስለዚህ በልበ ሙሉነት መቅጠር ይችላሉ።",
        why2_title: "ደህንነቱ የተጠበቀ መልእክት",
        why2_desc: "የግል ዝርዝሮችን ከመጋራትዎ በፊት በአፑ ውስጥ በደህንነት ይግባቡ።",
        why3_title: "የአካባቢ ባለሙያዎች",
        why3_desc: "ለፈጣን አገልግሎት በአካባቢዎ ውስጥ ተሰጥኦ ያግኙ።",
        
        // Download Section
        dl_title: "ለመጀመር ተዘጋጅተዋል?",
        dl_subtitle: "የ KOROSO አፕን ዛሬ ያውርዱ እና ማህበረሰቡን ይቀላቀሉ።",
        dl_button: "KOROSO APK ይጫኑ",
        dl_note: "Android 8.0+ ያስፈልጋል። ደህንነቱ የተጠበቀ እና አስተማማኝ ተከላ።",
        
        // Footer
        footer_copy: "&copy; 2024 KOROSO. መብቱ በህግ የተጠበቀ ነው።",
        
        // Modal
        modal_title: "KOROSO ይጫኑ",
        modal_intro: "አፑን ለመጫን እባክዎ እነዚህን ደረጃዎች ይከተሉ፡",
        modal_step1: 'ከታች ያለውን <strong>APK ያውርዱ</strong> ቁልፍ ይንኩ።',
        modal_step2: 'ከተጠየቁ፣ በአሳሽ ቅንብሮችዎ ውስጥ ከ<strong>"ያልታወቁ ምንጮች"</strong> መጫን ይፍቀዱ።',
        modal_step3: "KOROSO ለመጫን የወረደውን ፋይል ይክፈቱ።",
        modal_secure: "ደህንነትዎ ቅድሚያችን ነው። ፋይሉ ተፈትሷል።",
        modal_btn: "APK አሁን ያውርዱ",
        modal_downloading: "ማውረድ በማዘጋጀት ላይ...",
        modal_downloaded: "ተወርዷል! ማሳወቂያዎችን ይመልከቱ",
        modal_retry: "እንደገና ይሞክሩ",
        modal_android_only: "ለ Android ብቻ",
        modal_toast_title: "📲 ቀጣይ ደረጃዎች፡",
        modal_toast_steps: "1. <b>ማሳወቂያዎችዎን</b> ወይም <b>ማውረዶችን</b> ይክፈቱ<br>2. የ <b>KOROSO.apk</b> ፋይልን ይንኩ<br>3. ከተጠየቁ መጫን ይፍቀዱ<br>4. KOROSO ን ይክፈቱ እና መቅጠር ይጀምሩ!"
    }
};

// Current language (default: English)
let currentLang = 'en';

// Check for saved preference
const savedLang = localStorage.getItem('koroso-lang');
if (savedLang && translations[savedLang]) {
    currentLang = savedLang;
}

// Apply language on page load
document.addEventListener('DOMContentLoaded', () => {
    applyLanguage(currentLang);
});

// Toggle between languages
function toggleLanguage() {
    currentLang = currentLang === 'en' ? 'am' : 'en';
    localStorage.setItem('koroso-lang', currentLang);
    applyLanguage(currentLang);
}

// Apply translation to all elements with data-i18n attribute
function applyLanguage(lang) {
    // Update HTML lang attribute (for CSS font switching)
    document.documentElement.lang = lang;
    
    // Update all translatable elements
    document.querySelectorAll('[data-i18n]').forEach(el => {
        const key = el.getAttribute('data-i18n');
        if (translations[lang][key]) {
            el.innerHTML = translations[lang][key];
        }
    });
    
    // Update language toggle button label (show the OTHER language)
    const langLabel = document.getElementById('langLabel');
    if (langLabel) {
        langLabel.textContent = lang === 'en' ? 'አማርኛ' : 'English';
    }
    
    // Update page title
    document.title = lang === 'am' 
        ? 'KOROSO | ያገናኙ። ይቅጠሩ። ይቅጠሩ።'
        : 'KOROSO | Connect. Hire. Get Hired.';
    
    console.log(`Language switched to: ${lang}`);
}

// =====================================================
// --- END LANGUAGE SYSTEM ---
// =====================================================


// --- Three.js Space Background with Floating Profession Icons ---

const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
const renderer = new THREE.WebGLRenderer({ canvas: document.querySelector('#bg-canvas'), alpha: true });
renderer.setSize(window.innerWidth, window.innerHeight);

// 1. Create a Starfield (Tiny Particles)
const starGeometry = new THREE.BufferGeometry();
const starVertices = [];
for (let i = 0; i < 1500; i++) {
    starVertices.push(Math.random() * 50 - 25, Math.random() * 50 - 25, Math.random() * 50 - 25);
}
starGeometry.setAttribute('position', new THREE.Float32BufferAttribute(starVertices, 3));
const starMaterial = new THREE.PointsMaterial({ 
    color: 0x2ecc71, 
    size: 0.04, 
    transparent: true, 
    opacity: 0.4 
});
const stars = new THREE.Points(starGeometry, starMaterial);
scene.add(stars);

// 2. Icon List (Music Note Removed)
const iconList = [
    // --- CUSTOM SVG ICONS ---
    { type: 'svg', name: 'broom', path: 'M 70 10 L 58 60 L 62 60 L 74 10 Z M 45 60 Q 30 60 25 80 L 75 80 Q 70 60 55 60 Z M 25 80 L 20 95 L 80 95 L 75 80 Z' },
    { type: 'svg', name: 'mop', path: 'M 70 10 L 58 60 L 62 60 L 74 10 Z M 40 60 Q 40 80 30 90 L 70 90 Q 60 80 60 60 Z' },
    
    // --- FONT AWESOME ICONS ---
    { type: 'font', class: 'fa-wrench', char: '\uf0ad' },
    { type: 'font', class: 'fa-tools', char: '\uf7d9' },
    { type: 'font', class: 'fa-hammer', char: '\uf6e3' },
    { type: 'font', class: 'fa-paint-roller', char: '\uf5aa' },
    { type: 'font', class: 'fa-plug', char: '\uf1e6' },
    { type: 'font', class: 'fa-hard-hat', char: '\uf807' },
    { type: 'font', class: 'fa-car', char: '\uf1b9' },
    { type: 'font', class: 'fa-stethoscope', char: '\uf0f1' },
    { type: 'font', class: 'fa-user-doctor', char: '\uf0f0' },
    { type: 'font', class: 'fa-paw', char: '\uf1b0' },
    { type: 'font', class: 'fa-tooth', char: '\uf5c9' },
    { type: 'font', class: 'fa-eye', char: '\uf06e' },
    { type: 'font', class: 'fa-baby', char: '\uf77c' },
    { type: 'font', class: 'fa-calculator', char: '\uf1ec' },
    { type: 'font', class: 'fa-scale-balanced', char: '\uf24e' },
    { type: 'font', class: 'fa-laptop-code', char: '\uf5fc' },
    { type: 'font', class: 'fa-chart-line', char: '\uf201' },
    { type: 'font', class: 'fa-building', char: '\uf1ad' },
    { type: 'font', class: 'fa-envelope', char: '\uf0e0' },
    { type: 'font', class: 'fa-camera', char: '\uf030' },
    { type: 'font', class: 'fa-scissors', char: '\uf0c4' },
    { type: 'font', class: 'fa-utensils', char: '\uf2e7' },
    { type: 'font', class: 'fa-palette', char: '\uf53f' },
    { type: 'font', class: 'fa-dumbbell', char: '\uf44b' },
    { type: 'font', class: 'fa-seedling', char: '\uf4d8' },
    { type: 'font', class: 'fa-truck', char: '\uf0d1' },
    { type: 'font', class: 'fa-graduation-cap', char: '\uf19d' },
    { type: 'font', class: 'fa-ruler-combined', char: '\uf546' }
];

const floatingIcons = [];
const iconCount = 120;

// Function to generate a texture from a FontAwesome character
function createFontTexture(charCode) {
    const canvas = document.createElement('canvas');
    canvas.width = 128;
    canvas.height = 128;
    const ctx = canvas.getContext('2d');
    
    ctx.clearRect(0, 0, 128, 128);
    ctx.font = '900 100px "Font Awesome 6 Free"';
    ctx.fillStyle = '#F4C430';
    ctx.textAlign = 'center';
    ctx.textBaseline = 'middle';
    
    ctx.fillText(charCode, 64, 64);
    
    const texture = new THREE.CanvasTexture(canvas);
    texture.needsUpdate = true;
    return texture;
}

// Function to generate a texture from a Custom SVG Path
function createSvgTexture(svgPath) {
    const canvas = document.createElement('canvas');
    canvas.width = 128;
    canvas.height = 128;
    const ctx = canvas.getContext('2d');
    
    ctx.clearRect(0, 0, 128, 128);
    
    const path = new Path2D(svgPath);
    ctx.fillStyle = '#F4C430';
    ctx.fill(path);
    
    const texture = new THREE.CanvasTexture(canvas);
    texture.needsUpdate = true;
    return texture;
}

// Initialize Scene AFTER Fonts are Loaded
document.fonts.ready.then(() => {
    console.log("Fonts loaded. Initializing " + iconCount + " floating icons...");
    initFloatingIcons();
});

function randomRange(min, max) {
    return Math.random() * (max - min) + min;
}

function initFloatingIcons() {
    for (let i = 0; i < iconCount; i++) {
        const randomIcon = iconList[Math.floor(Math.random() * iconList.length)];
        
        let texture;
        if (randomIcon.type === 'svg') {
            texture = createSvgTexture(randomIcon.path);
        } else {
            texture = createFontTexture(randomIcon.char);
        }
        
        const material = new THREE.SpriteMaterial({ 
            map: texture, 
            transparent: true,
            opacity: 0.85,
            color: 0xffffff,
            depthTest: false 
        });
        
        const sprite = new THREE.Sprite(material);
        
        // Spawn from Random Edges
        const edge = Math.floor(Math.random() * 4);
        const spawnRange = 50;

        if (edge === 0) {
            sprite.position.x = -spawnRange;
            sprite.position.y = randomRange(-spawnRange, spawnRange);
        } else if (edge === 1) {
            sprite.position.x = spawnRange;
            sprite.position.y = randomRange(-spawnRange, spawnRange);
        } else if (edge === 2) {
            sprite.position.y = spawnRange;
            sprite.position.x = randomRange(-spawnRange, spawnRange);
        } else {
            sprite.position.y = -spawnRange;
            sprite.position.x = randomRange(-spawnRange, spawnRange);
        }
        
        sprite.position.z = randomRange(-15, 15);
        
        const scale = randomRange(1.0, 3.0); 
        sprite.scale.set(scale, scale, 1);
        
        const speed = randomRange(0.02, 0.07);
        const angle = Math.random() * Math.PI * 2;
        
        sprite.userData = {
            velocity: new THREE.Vector3(
                Math.cos(angle) * speed, 
                Math.sin(angle) * speed, 
                0
            ),
            rotationSpeed: randomRange(-0.02, 0.02)
        };
        
        scene.add(sprite);
        floatingIcons.push(sprite);
    }
}

camera.position.z = 12;

function animate() {
    requestAnimationFrame(animate);
    
    stars.rotation.y += 0.0002;
    stars.rotation.x += 0.0001;
    
    const boundary = 55; 
    
    floatingIcons.forEach(icon => {
        icon.position.x += icon.userData.velocity.x;
        icon.position.y += icon.userData.velocity.y;
        icon.material.rotation += icon.userData.rotationSpeed;
        
        if (Math.abs(icon.position.x) > boundary || Math.abs(icon.position.y) > boundary) {
            const edge = Math.floor(Math.random() * 4);
            const spawnRange = 50;
            
            if (edge === 0) {
                icon.position.x = -spawnRange;
                icon.position.y = randomRange(-spawnRange, spawnRange);
            } else if (edge === 1) {
                icon.position.x = spawnRange;
                icon.position.y = randomRange(-spawnRange, spawnRange);
            } else if (edge === 2) {
                icon.position.y = spawnRange;
                icon.position.x = randomRange(-spawnRange, spawnRange);
            } else {
                icon.position.y = -spawnRange;
                icon.position.x = randomRange(-spawnRange, spawnRange);
            }
            
            const speed = randomRange(0.02, 0.07);
            const angle = Math.random() * Math.PI * 2;
            icon.userData.velocity.set(
                Math.cos(angle) * speed, 
                Math.sin(angle) * speed, 
                0
            );
        }
    });
    
    renderer.render(scene, camera);
}
animate();

window.addEventListener('resize', () => {
    camera.aspect = window.innerWidth / window.innerHeight;
    camera.updateProjectionMatrix();
    renderer.setSize(window.innerWidth, window.innerHeight);
});


// --- Mobile Menu Toggle ---
function toggleMobileMenu() {
    const navLinks = document.querySelector('.nav-links');
    navLinks.classList.toggle('active');
}

document.querySelectorAll('.nav-links a').forEach(link => {
    link.addEventListener('click', () => {
        document.querySelector('.nav-links').classList.remove('active');
    });
});


// --- Smooth Scrolling ---
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const targetId = this.getAttribute('href');
        if (targetId === '#') return;
        
        const targetElement = document.querySelector(targetId);
        if (targetElement) {
            const headerOffset = 80;
            const elementPosition = targetElement.getBoundingClientRect().top;
            const offsetPosition = elementPosition + window.pageYOffset - headerOffset;

            window.scrollTo({
                top: offsetPosition,
                behavior: "smooth"
            });
        }
    });
});


// --- Download Modal Logic ---
const modal = document.getElementById('downloadModal');
const downloadBtn = document.getElementById('actualDownloadBtn');

// IMPORTANT: Replace with your actual secure backend endpoint
// Never expose the direct APK URL in client-side code
const SECURE_DOWNLOAD_ENDPOINT = "https://your-server.com/api/download/koroso"; 

function openDownloadModal() {
    modal.style.display = 'flex';
    document.body.style.overflow = 'hidden';
}

function closeDownloadModal() {
    modal.style.display = 'none';
    document.body.style.overflow = 'auto';
}

window.onclick = function(event) {
    if (event.target == modal) {
        closeDownloadModal();
    }
}

// Handle the download with feedback and language support
downloadBtn.addEventListener('click', async function() {
    const originalText = this.innerHTML;
    const t = translations[currentLang];
    
    // Step 1: Detect device
    const isAndroid = /android/i.test(navigator.userAgent);
    
    if (!isAndroid) {
        this.innerHTML = `<i class="fas fa-exclamation-triangle"></i> ${t.modal_android_only}`;
        setTimeout(() => {
            this.innerHTML = originalText;
        }, 3000);
        return;
    }
    
    // Step 2: Show loading state
    this.innerHTML = `<i class="fas fa-spinner fa-spin"></i> ${t.modal_downloading}`;
    this.disabled = true;
    
    // Step 3: Simulate fetching a secure temporary download URL
    // In production, this should be a real fetch to your backend
    setTimeout(() => {
        // Trigger download
        const link = document.createElement('a');
        link.href = SECURE_DOWNLOAD_ENDPOINT;
        link.download = 'KOROSO.apk';
        link.style.display = 'none';
        document.body.appendChild(link);
        link.click();
        document.body.removeChild(link);
        
        // Step 4: Show success message
        this.innerHTML = `<i class="fas fa-check"></i> ${t.modal_downloaded}`;
        
        // Step 5: Show toast with next steps
        setTimeout(() => {
            showInstallInstructions();
            closeDownloadModal();
            this.innerHTML = originalText;
            this.disabled = false;
        }, 1500);
        
    }, 1000);
});

// Show install instructions toast (bilingual support)
function showInstallInstructions() {
    const t = translations[currentLang];
    
    const toast = document.createElement('div');
    toast.className = 'toast-notification';
    toast.style.cssText = `
        position: fixed;
        bottom: 30px;
        left: 50%;
        transform: translateX(-50%);
        background: #1B4D3E;
        color: white;
        padding: 20px 30px;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(46, 204, 113, 0.4);
        z-index: 3000;
        max-width: 90%;
        text-align: center;
        border: 1px solid #2ecc71;
        font-family: 'Plus Jakarta Sans', sans-serif;
        animation: slideUp 0.3s ease-out;
    `;
    toast.innerHTML = `
        <strong style="color: #F4C430;">${t.modal_toast_title}</strong><br>
        <small>${t.modal_toast_steps}</small>
    `;
    document.body.appendChild(toast);
    
    setTimeout(() => {
        toast.style.transition = 'opacity 0.5s ease';
        toast.style.opacity = '0';
        setTimeout(() => toast.remove(), 500);
    }, 8000);
}


// --- Scroll Animations ---
const observerOptions = {
    threshold: 0.1,
    rootMargin: "0px 0px -50px 0px"
};

const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.style.opacity = '1';
            entry.target.style.transform = 'translateY(0)';
            observer.unobserve(entry.target);
        }
    });
}, observerOptions);

document.querySelectorAll('.step-card, .service-card, .feature-item').forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(30px)';
    el.style.transition = 'opacity 0.6s ease-out, transform 0.6s ease-out';
    observer.observe(el);
});